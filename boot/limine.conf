# Limine bootloader configuration for zeroxos.
# Syntax: Limine 8.x+ (`limine.conf`, `path:` with a `boot():` resource URI).

timeout: 0

/zeroxos v0.1
    protocol: limine
    path: boot():/boot/zeroxos
